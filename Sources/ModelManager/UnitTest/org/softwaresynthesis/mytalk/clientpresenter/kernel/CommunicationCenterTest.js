/**
 * Verifica del Monolite
 * 
 * @author Diego Beraldin
 * @version 2.0
 */
module("CommunicationCenterTest", {
	setup : function() {
		// oggetto da testare
		monolith = new CommunicationCenter();
	},
	teardown : function() {
	}
});

/**
 * Verifica il comportamento del metodo connect
 * 
 * @author Diego Beraldin
 * @version 2.0
 */
test("testConnect()", function() {
	var i = 0;
	
	expect(i);
});